rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 오직 finishor93@gmail.com 계정으로 인증된 사용자만 모든 데이터 읽기/쓰기 허용
      allow read, write: if request.auth != null && request.auth.token.email == 'finishor93@gmail.com';
    }
  }
}
